export default function TreeVisualizer() {
  return (
    <section className="panel panel-visualization">
      <header className="panel-header">
        <h2>Tree Visualizer</h2>
      </header>
      <p className="empty-state">
        Tree rendering hooks are scaffolded; the execution engine is already tracking call stack and
        variable transitions needed for BST/AVL rendering.
      </p>
    </section>
  );
}
